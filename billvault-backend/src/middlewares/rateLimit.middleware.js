/**
 * @file middlewares/rateLimit.middleware.js
 * @description Rate limiting using express-rate-limit with Redis store (rate-limit-redis). Exports: authLimiter, uploadLimiter, searchLimiter, generalLimiter, otpLimiter.
 *
 * @layer Middleware
 * @module rateLimit
 */